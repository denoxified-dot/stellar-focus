{
  "name": "stellar-focus",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A study timer set against a procedurally generated starfield and spiral galaxy.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-data": "node scripts/fetch-exoplanets.mjs",
    "fetch-apod": "node scripts/fetch-apod.mjs",
    "fetch-messier": "node scripts/fetch-messier.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
